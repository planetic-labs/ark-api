{
  "name": "@ark/api",
  "version": "2026.5.23",
  "private": true,
  "scripts": {
    "dev": "docker compose -f infra/docker-compose.yml up api",
    "lint": "uv run ruff check .",
    "test": "uv run pytest"
  }
}
